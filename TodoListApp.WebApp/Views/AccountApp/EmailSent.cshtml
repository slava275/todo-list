@{
    ViewData["Title"] = "Email Sent";
    ViewData["HideNavBar"] = true;
}

<div class="row justify-content-center">
    <div class="col-md-4 mt-5">
        <div class="card shadow-sm">
            <div class="card-body">
                <h3 class="text-center">Відновлення пароля</h3>
                <div class="text-center m-2">
                    На вашу електронну адресу було надіслано лист з інструкціями для відновлення пароля.
                </div>
                <div class="text-center mt-3">
                    <a asp-action="Login" class="btn btn-outline-primary w-100">Повернутися до входу</a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial.cshtml");
    }
}