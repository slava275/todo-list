@using TodoListShared.Models
@model TodoListModel

@{
    ViewData["Title"] = "Edit Todo";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">@ViewData["Title"]</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Edit" method="post">

                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="@Model.Title" for="Title" class="form-label">Назва списку</label>
                            <input asp-for="@Model.Title" class="form-control" id="Title" placeholder="@Model.Title" />
                            <span asp-validation-for="@Model.Title" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="@Model.Description" for="Description" class="form-label">Опис</label>
                            <textarea asp-for="@Model.Description" id="Description" class="form-control" rows="3" placeholder="@Model.Description"></textarea>
                            <span asp-validation-for="@Model.Description" class="text-danger"></span>
                        </div>

                        <div class="d-flex justify-content-between align-items-center mt-4">
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left"></i> Назад до списку
                            </a>
                            <button type="submit" class="btn btn-success">
                                <i class="bi bi-check-lg"></i> Зберегти
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}